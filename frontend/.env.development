# Ambiente di SVILUPPO locale (next dev). Caricato automaticamente da Next.js
# solo con `npm run dev`; la produzione usa il Dockerfile e non lo legge.
# /api/* viene inoltrato al FastAPI di sviluppo sull'host (porta 8002,
# NON la 8088 del container di produzione, né la 8001 — occupata da un altro servizio).
# Il login ai4auth fa ritornare qui dopo l'accesso, invece che sul sito di produzione.
BACKEND_ORIGIN=http://127.0.0.1:8002
NEXT_PUBLIC_APP_URL=http://localhost:3107
# Identità di sviluppo iniettata dal proxy (frontend/src/proxy.ts) verso il
# backend dev: bypassa ai4auth perché il cookie di autenticazione è sul
# dominio .ai4educ.org e localhost non può presentarlo.
DEV_AUTH_SECRET=dev-local-only
DEV_AUTH_USER=nugh75
DEV_AUTH_NAME=Daniele (dev)
DEV_AUTH_GROUPS=admins,docenti,studenti
