import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Linea del tempo - CounselorBot' };

export default function TimelineLayout({ children }: { children: React.ReactNode }) {
    return children;
}
