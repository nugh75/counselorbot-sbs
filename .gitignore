# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env
.env.bak
.env.bak-*
.venv/

# Node
node_modules/
.next/
dist/

# Project specific
.tmp/
.DS_Store
/directives
/execution

# Legacy SQLite dev DB (prod uses Postgres)
counselorbot.db
*.db

# Runtime data with student PII — never commit
uploads/
backend/graphify-out/
graphify-out/

# Runtime session data (root-owned, session/PII) — never commit
session_memory/

# Dynamic RAG collections uploaded at runtime — never commit
rag-collections/

# Indice RAG rigenerato in locale (lock + cache di embedding): non versionarlo.
.rag_index/
